// align_asm_colon

int main( int argc, char * argv[] )
{
    int src = 1;
    int dst = 0;

    asm volatile
    (
        "mov %1, %0\n"
        "add $1, %0"

        : "=r" ( dst )
        : "r"  ( src )
    );

    return 0;
}
